import React from 'react'

import { Loading as Loader } from "@/components"

const loading = () => {
  return (
    <div className="body w-full h-screen flex-center"><Loader/></div>
  )
}

export default loading